{
  "name": "paradise-shop",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build && cp -r dist/* .",
    "start": "node server/vercel.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  }
}
